Single Alembic history for brasaland-m5.

- Generate / upgrade against disposable Postgres only (never upgrade live m5
  until a revision is reviewed).
- Stamp live m5 after a baseline when the live schema already matches models.
- After baseline, reviewed revisions use `upgrade head` on live m5 (first:
  `d38c62feec63` RFP unique constraints); stamp remains for baseline-only cases.
