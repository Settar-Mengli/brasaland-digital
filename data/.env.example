# Copy to data/.env for local pipeline runs. Same brasaland-m5 DATABASE_URL as inventory/telemetry.
# Use Python 3.13: uv sync --python 3.13 / uv run --python 3.13 (bare 3.14 fails building pydantic-core without MSVC).
DATABASE_URL=
