# Copy to .env.local — same-origin proxy base (see next.config.ts rewrites → incident API :8011)
NEXT_PUBLIC_INCIDENTS_API_URL=http://localhost:3004/api/incidents

# Server-side rewrite target (optional; defaults to localhost backend for native dev; overridden in Docker compose)
# INCIDENTS_API_ORIGIN=http://localhost:8011
