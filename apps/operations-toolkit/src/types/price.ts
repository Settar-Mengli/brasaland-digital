/** Represents a monetary amount expressed in both of Brasaland's operating currencies. */
export interface Price {
  /** Amount in United States Dollars. */
  USD: number;
  /** Amount in Colombian Pesos. */
  COP: number;
}
