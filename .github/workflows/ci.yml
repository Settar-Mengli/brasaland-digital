name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  uv-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        dir:
          [
            services/auth,
            packages/auth-verify,
            packages/shared,
            services/supplier-directory,
            services/incident-analysis,
            services/incident-manager,
            services/inventory,
            services/telemetry,
            services/reporting,
            services/knowledge,
            services/rfp,
            mcps/company-tools,
          ]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          python-version: '3.13'
      - name: Run tests
        run: uv run pytest
        working-directory: ${{ matrix.dir }}

  pipelines-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          python-version: '3.13'
      - name: Run pipeline tests
        run: uv run --directory data --python 3.13 pytest

  ruff:
    name: Ruff baseline ratchet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          python-version: '3.13'
      - name: Reject new Ruff violations
        run: >-
          uv run --directory data --locked --only-group dev --python 3.13
          python ../scripts/check_ruff_baseline.py

  route-auth-audit:
    name: Route auth OpenAPI audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          python-version: '3.13'
      - name: Reject unallowlisted open routes
        run: python scripts/audit_route_auth.py

  node-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        workspace:
          ['@brasaland/operations-toolkit', '@brasaland/backoffice', '@brasaland/incident-manager']
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npm run test --workspace ${{ matrix.workspace }}

  eslint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Lint TypeScript workspaces
        run: npm run lint

  node-typecheck:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          [
            {
              workspace: '@brasaland/operations-toolkit',
              command: 'npm run typecheck --workspace @brasaland/operations-toolkit',
              working-directory: '.',
            },
            {
              workspace: '@brasaland/backoffice',
              command: 'npx tsc --noEmit',
              working-directory: 'uis/backoffice',
            },
            {
              workspace: '@brasaland/website',
              command: 'npx tsc --noEmit',
              working-directory: 'uis/website',
            },
            {
              workspace: '@brasaland/incident-manager',
              command: 'npx tsc --noEmit',
              working-directory: 'uis/incident-manager',
            },
          ]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Typecheck
        run: ${{ matrix.command }}
        working-directory: ${{ matrix.working-directory }}

  format-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Check formatting
        run: npm run format:check

  celery-routing:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          python-version: '3.13'
      - name: Sync reporting deps
        run: uv sync --python 3.13
        working-directory: services/reporting
      - name: Sync rfp deps
        run: uv sync --python 3.13
        working-directory: services/rfp
      - name: Prove queue isolation on real Redis
        env:
          REDIS_URL: redis://localhost:6379/0
          CELERY_ISOLATION_ALLOW_FLUSH: '1'
        run: uv run --python 3.13 python ../../scripts/test_celery_queue_isolation.py
        working-directory: services/reporting
